# 已儲存卡片清單

## 描述
結帳頁面付款表單中，顯示消費者已儲存的信用卡選項。

## 元素
- Radio 選項列表：每張已儲存卡片一個 radio button
  - 格式：`**** **** **** {last4} (到期: {MM}/{YY})`
  - 預設卡片自動選中
- 「使用新卡片」radio 選項（最後一個）
- 選擇已儲存卡片時，隱藏新卡片輸入表單（CardNo/CardExp 隱藏，CVC 可能仍需輸入）
- 選擇「使用新卡片」時，顯示新卡片輸入表單

## 觸發條件
- 記憶卡號功能啟用 AND 消費者已登入 AND 有已儲存卡片
