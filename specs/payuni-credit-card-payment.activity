Activity: PayUni v1 信用卡付款
Description: 消費者透過 PayUni v1 閘道（站內輸入卡號）完成信用卡付款

Actors:
  - Consumer: 結帳頁面的購物消費者
  - WooCommerce: WooCommerce 結帳核心，管理訂單與付款流程
  - CreditGateway: PAYUNI\Gateways\Credit 閘道類別（id: payuni-credit）
  - RequestBuilder: PAYUNI\Gateways\Request 類別，組裝加密 API 請求
  - PayUniAPI: PayUni 外部 API 端點（api/credit）
  - ResponseHandler: PAYUNI\Gateways\Response 類別，處理 API 回應與訂單狀態更新
  - PaymentAPI: PAYUNI\APIs\Payment 類別，提供加解密與日誌功能

Triggers:
  - Consumer 在結帳頁選擇「統一金流 PAYUNi 信用卡」付款方式並點擊下單

Flow:
  1. Consumer 在結帳頁面看到信用卡表單欄位（卡號、有效期限、安全碼）
     → Hook: woocommerce_credit_card_form_start (do_action)
     → Hook: woocommerce_credit_card_form_fields (apply_filters)
     → Hook: woocommerce_credit_card_form_end (do_action)

  2. Consumer 填寫信用卡資料並點擊下單按鈕
     → WooCommerce 呼叫 CreditGateway::validate_fields()

  3. CreditGateway 驗證表單欄位
     → Condition: payment_method 是否為 payuni-credit
       Yes → 繼續驗證
       No → return false
     → Condition: 是否使用已儲存的 Token（wc-payuni-credit-payment-token 為數字）
       Yes → return true（跳過卡號驗證）
       No → 驗證 card-number / card-expiry / card-cvc 是否為空

  4. WooCommerce 呼叫 CreditGateway::process_payment($order_id)
     → CreditGateway 實例化新的 self() 並呼叫 get_card_data() 取得卡片資料
     → 建立 Request 實例，傳入 CreditGateway

  5. RequestBuilder 組裝交易參數（get_transaction_args）
     → 參數：MerID, MerTradeNo(order_id + suffix), TradeAmt, Timestamp, UsrMail, ProdDesc
     → Condition: 有卡片資料且為信用卡閘道
       Yes → 加入 CardNo, CardExpired, CardCVC
     → Condition: 是否勾選「儲存付款資訊」或使用已存 Token
       Yes → 加入 CreditToken（客戶 email）
     → Condition: 是否使用已存 Token（token_id 為數字）
       Yes → 以 CreditHash 取代 CardNo/CardExpired/CardCVC
     → Condition: 是否啟用 3D 驗證（payuni_3d_auth option）
       Yes → 加入 API3D=1, ReturnURL=home_url('wc-api/payuni_notify_card')
       → 儲存 _payuni_is_3d_auth = 'yes' 到訂單 meta
     → Hook: payuni_transaction_args_payuni-credit (apply_filters)

  6. RequestBuilder 加密交易參數
     → 使用 Payment::encrypt() 以 AES-256-GCM 加密
     → 使用 Payment::hash_info() 產生 SHA-256 雜湊
     → 組成 {MerID, Version:'1.0', EncryptInfo, HashInfo}

  7. RequestBuilder 發送 POST 請求到 PayUniAPI
     → URL: {api_url}/api/credit
     → 方法: wp_remote_request，timeout=60

  8. PayUniAPI 回傳加密回應

  9. RequestBuilder 解密回應（Payment::decrypt）
     → 使用 Response::get_formatted_decrypted_data() 格式化

  10. RequestBuilder 判斷交易結果
      → Condition: Status 是否為 'SUCCESS'
        No → 顯示錯誤訊息（wc_add_notice）
          → Condition: Status 是否為 'CREDIT04001'（相同訂單編號）
            Yes → 呼叫 woomp_copy_order() 建立新訂單，遞迴呼叫 process_payment
        Yes → 繼續
      → Condition: 是否為 3D 驗證
        Yes → return {result:'success', redirect: $data['URL']}（導向 3D 驗證頁面）
        No → 呼叫 set_response() 處理回應

  11. ResponseHandler::card_response() 處理信用卡回應
      → 解密 EncryptInfo
      → 格式化交易資料
      → Condition: Status 是否為 'SUCCESS'
        Yes → 呼叫 $order->payment_complete()
          → 儲存交易資訊到訂單 meta（_payuni_resp_status, _payuni_resp_message, _payuni_resp_trade_no 等）
          → Condition: 是否需要儲存卡號（_payuni_token_maybe_save 或 hash request）
            Yes → 呼叫 save_card_to_payment_method() 建立 WC_Payment_Token_CC
          → 更新訂單狀態為 processing
        No → 更新訂單狀態為 failed

  12. Consumer 被導向訂單完成頁面（order-received）

Side Effects:
  - 訂單 meta 儲存交易回應資料（_payuni_resp_*）
  - 訂單 meta 儲存 _payuni_order_suffix（遞增避免重複編號）
  - 若勾選儲存卡號，建立 WC_Payment_Token_CC 到 wp_woocommerce_payment_tokens
  - 若為 hash request（5元扣款），排程 Action Scheduler 任務延遲 2 分鐘取消授權
  - 訂單備註記錄交易詳情
  - WC Logger 記錄交易日誌（來源: payuni_payment）

Related Features:
  - payuni-credit-card-payment.feature
  - payuni-tokenization.feature
  - payuni-refund.feature
