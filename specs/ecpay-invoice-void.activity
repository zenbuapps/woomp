Activity: 綠界電子發票作廢
Description: 透過綠界 API 作廢已開立的電子發票，支援自動與手動兩種模式。

Actors:
  - 店家管理員: 在後台手動觸發發票作廢或設定自動作廢規則
  - WooCommerce: 訂單狀態變更時觸發自動作廢
  - 綠界 API: 接收發票作廢請求並回傳結果（Invoice/IssueInvalid endpoint）

Triggers:
  - 自動模式：訂單狀態變更為管理員設定的指定狀態（wc_woomp_ecpay_invoice_invalid_at）
  - 手動模式：管理員在訂單列表或訂單編輯頁點擊「作廢發票」按鈕

Flow:
  1. [WooCommerce] 訂單狀態變更或 [店家管理員] 點擊作廢按鈕
     → Hook (自動): woocommerce_order_status_{$invoice_invalid_at}
       → 呼叫 Admin::invalid_invoice($order_id)
     → Hook (手動): wp_ajax_invalid_invoice
       → 呼叫 Ajax::invalid_invoice()

  2. [系統] 檢查發票是否已開立
     → Condition (自動模式): _ecpay_invoice_status === '1'
       Yes → 繼續作廢
       No → 中止
     → Condition (手動模式): 直接檢查 order_id 是否有效

  3. [EcpayInvoiceHandler] 呼叫 invalid_invoice($order_id)

  4. [EcpayInvoiceHandler] 取得發票號碼
     → 從 _ecpay_invoice_number meta 讀取 invoice_number

  5. [EcpayInvoiceHandler] 檢查發票狀態
     → Condition: _ecpay_invoice_status === 1
       Yes → 繼續
       No → 回傳「Invalid Ecpay invoice error!」

  6. [EcpayInvoiceHandler] 設定 SDK 參數
     → Invoice_Method = 'INVOICE_VOID'
     → Invoice_Url = API 作廢 URL
     → InvoiceNumber = 已開立的發票號碼
     → Reason = '發票作廢'

  7. [EcpayInvoiceHandler] 透過 ECPay_Woo_EcpayInvoice 呼叫 Check_Out()
     → API URL (測試): https://einvoice-stage.ecpay.com.tw/Invoice/IssueInvalid
     → API URL (正式): https://einvoice.ecpay.com.tw/Invoice/IssueInvalid

  8. [綠界 API] 回傳結果

  9. [EcpayInvoiceHandler] 處理回傳結果
     → 在訂單備註寫入作廢結果（發票號碼、訊息）
     → Condition: RtnCode === '1' 或 '5070453'（已作廢）
       Yes → 更新 _ecpay_invoice_status = 0, 清除 _ecpay_invoice_number
       No → 僅寫入備註

  10. [Ajax] 回傳 JSON 結果給前端（手動模式）
      → 前端 admin.js 先彈出確認框「確定要刪除此筆發票」
      → 確認後送出 AJAX 請求
      → 顯示結果 alert 並 reload 頁面

Side Effects:
  - 訂單 meta 更新：_ecpay_invoice_status = 0, _ecpay_invoice_number = ''
  - 訂單備註新增作廢結果

Related Features:
  - ecpay-invoice-void.feature
  - ecpay-invoice-issue.feature
  - invoice-settings.feature
